# backend/.env.gcp.example
# Environment variables for GCP backend deployment
# These should be stored as Google Cloud Secrets, not in files

NODE_ENV=production

# Database
MONGO_URI=mongodb+srv://yogeshramakrishnan:pnSCE8RtcPqPetdV@cluster0.qar08.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
DB_NAME=sms_db
# Security
JWT_SECRET=9e2a7f4d1c8b5e3a9f7d2c6b8e4a1f9d3c7b5e2a8f6d4c1b7e9a3f5d8c2b6e4
JWT_EXPIRES_IN=1d

# CORS (will be updated by deployment script)
CORS_ORIGIN=https://bala-vihar-sms-612621646571.us-east1.run.app,http://localhost:5173,http://localhost:3000

# # Email (if using email features)
# EMAIL_SERVICE=gmail
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASS=your-app-password

# File uploads (if using file uploads)
MAX_FILE_SIZE=10485760
ALLOWED_FILE_TYPES=image/jpeg,image/png,application/pdf

# Logging
LOG_LEVEL=info